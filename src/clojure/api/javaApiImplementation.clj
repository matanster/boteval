;;; implementation of the api for Java users (stub)

(ns api.implemenntaion
  (:import [org.boteval.api ApiInterface]))

; should use defrecord or deftype, or we can implement the interface in Java rather than in clojure
